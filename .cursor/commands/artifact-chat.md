# /artifact-chat — answer or revise a captured artifact

Read and follow:
- `.claude/commands/artifact-chat.md`
- `.claude/agents/brainstorm-orchestrator.md`

Route questions to a general subagent; route SVG revisions to **svg-artisan**. Use `capture_artifact` with `revises` and `reply_artifact_chat` for delivery.
