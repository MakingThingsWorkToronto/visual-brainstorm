# Predicted Human-Journeys Registry

This file is the additive registry of every predicted human journey through Visual Brainstorm.

**The rule.** Predict EVERY human journey before you test it. Audit ADDITIVELY — new journeys are
APPENDED, existing rows are never removed (a walked-away journey becomes a documented coverage
gap, not a deletion). At EACH surface a journey walks, assert the SPECIFIC canonical DATA is
visibly in frame — not a 200, not a testid, not "N rows" (reject error/blank/spinner/empty-data
false-greens; helper: `scripts/lib/visual-honesty.mjs`). Every proof runs against the REAL
bridge/studio/browser and REAL MCP-tool pathways — NO mocks, NO preview/fixture harness. NEVER
fake the orchestrator to manufacture a surface: a test that calls a surface's producer itself
(e.g. `bridge.presentGallery`) proves nothing about a real run — the surface must arise from the
REAL path, reachable by REAL navigation. Mark each canonical-data assertion DONE or OWED.

**The real route (real-routes-human-sim-2026-07-09).** Every `human-sim*` journey spawns the
BUILT stdio MCP server (`scripts/lib/sim-runner.mjs` + `scripts/lib/mcp-client.mjs`) and plays
the orchestrator with real `tools/call` requests — `open_studio`, `ask_concierge`,
`present_gallery`, `present_board` (incl. the park→`reply_artifact_chat`→`rearmBoardId` detour),
`capture_artifact`, `session_status` — so the tool layer's intake lock, validation, digest
construction, and command routing are live in every run. What still needs a model in the loop is
only the AUTHORING of content (options, replies' prose, rationale); the delivery pathways are
proven. `scripts/ui-break-sweep.mjs` and `scripts/latency-profile.mjs` are NOT journeys — they
are a UI fuzzer and a same-process profiler on the bridge-integration layer (like
`scripts/smoke.mjs`, the Bridge is their subject).

| Journey | Surfaces walked | Canonical data asserted at each surface | Real-path proof (runnable command) | Faked-orchestrator risk |
|---|---|---|---|---|
| **1. Intake** | brief submit → intake-preparing veil → concierge Q&A → Living Gallery → mind map canvas | Gallery visibly shows the four cards "Mind map / Funnel / Wreck / Cluster" + the recommended reason-chip text; canvas shows the tree node topics ("Neon glyph / Mark / Motion"); the `intake-preparing` veil (not the panel) holds after submit — **DONE** (`scripts/human-sim.mjs`, `assertSurfaceShowsCanonical`) | `npm run test:human` (23 steps, gated in `npm test`) | **RESOLVED (was HIGH, then MITIGATED) — 2026-07-09.** The harness no longer stands in for anything: the journey issues the REAL `open_studio` (blocked → resolved by the brief), `ask_concierge`, `present_gallery`, and `present_board` tool calls, and the intake gate is proven LIVE in-run (`present_board` refused with "Intake incomplete" before the pick) on top of `tests/intake-gate.test.mjs`. Remaining OWED: only the live-model AUTHORING of concierge questions/gallery minis. |
| **2. Board round** | diverge board → select option → elaborate → submit | Board shows option labels "Alpha / Beta / Gamma" + the board title renders — **DONE** | `npm run test:human` (goal run, gated in `npm test`) | Low — board arrives via the real `present_board` TOOL (2026-07-09: option/axes validation live; the response returns as the tool's `feedbackDigest` + `response`, asserted); the gallery→board hand-off is real, not injected. |
| **3. Artifact keep** | keep artifact → fullscreen viewer → pin | Kept artifact's title/label visible in the viewer ("Beta keeper" + zoom/Notes/composer signature); pinned state visibly reflected (📌 Pinned toggle + `/api/state.pinnedSlugs` + the filmstrip row) — **DONE 2026-07-09** (the artifact arises from the REAL `capture_artifact` tool in the goal run, not a seeded state row) | `npm run test:human` (keep → viewer → pin steps, gated in `npm test`) | Low — real capture_artifact provenance, real POST /api/pinned round-trip. |
| **3b. Live artifact chat (send → own bubble)** | live view → click a keep (and a previous-round option) → ArtifactFullscreen composer → type + Send | The user's OWN message bubble renders in the dialog IMMEDIATELY (optimistic echo, then reconciled with the persisted copy — never dependent on WS timing) AND persists to the live thread's `artifacts/chat.jsonl` under both the artifact slug and an `option:<boardId>:<optionId>` slug — **DONE** (`scripts/human-sim-livechat.mjs`, 5 steps). Fixes the 2026-07-09 report "I submit a message but do not see my chat message bubble" | `npm run test:human:livechat` (gated in `npm test`) | Low — seeds a real live thread + keep on disk, BINDS it through the real `present_board` rearm route (2026-07-09), drives the real studio + real POST /api/artifact-chat; the bubble is asserted from real page text and the persistence from `/api/state` + a disk `SessionStore.open`. NEW 2026-07-09: the queued command is collected via the real `session_status` and Claude's answer DELIVERED via the real `reply_artifact_chat` — its bubble asserted in-frame. Only the reply's AUTHORING needs a live model. |
| **3c. Live board option chat (dials persist)** | live ACTIVE board → move a dial → open an option fullscreen (SAME viewer + chat) → type + Send | The composer is present on the CURRENT option set; the question persists under the `option:<boardId>:<optionId>` slug; the board STAYS live and the moved dial is UNCHANGED after the chat (non-destructive artifact-chat detour — the board never unmounts); the in-progress answer (dial) is persisted to `/api/state` drafts / `round-NN/draft.json` (generation meta recall) — **DONE** (`scripts/human-sim-boardchat.mjs`, 4 steps). Backend: `tests/board-draft.test.mjs` proves recordBoardDraft persist/reload, `/api/board-draft` 200/404/400, and the non-destructive park (board stays, no park recorded, draft survives) on the real bridge | `npm run test:human:boardchat` + `node --test tests/board-draft.test.mjs` (both gated in `npm test`) | Low — 2026-07-09: the board is presented by the REAL blocked `present_board` tool; the chat PARKS it (the synthetic `action:"park"` + `commands:["artifact-chat"]` response asserted AT the orchestrator), the answer is delivered via the real `reply_artifact_chat` (bubble in-frame), the SAME board re-arms via `rearmBoardId`, and a real submit returns the pre-detour dial in `response.axisValues` — the full documented detour contract, end to end. Only the reply's authoring needs a live model. |
| **3d. Mind-map maximize→chat + model-legible persistence** | live mind map → **REAL engine edit (mind-elixir addChild)** → maximize icon → unified fullscreen (SVG + chat right) → type + Send | The mind-map box shows a MAXIMIZE control; a REAL engine `addChild` lands on the canvas BEFORE maximizing (the step waits for the LAZY-LOADED engine instance on the container, never samples once); maximize FLUSHES the live draft — `round-NN/draft.json.editedTree` carries the new node and `tree.md` refreshes to the "Live tree … (in progress)" heading CONTAINING it (exactly what `/read-mindmap` and an artifact-chat answer read mid-edit); the fullscreen shows the mindmap-aware chat hint ("Claude reads your CURRENT tree") in-page; clicking opens the SAME `ArtifactFullscreen` as any artifact (chat docked right); a question persists under the mindmap SNAPSHOT artifact slug and the board STAYS live (non-destructive); the tree is persisted MODEL-LEGIBLY to `round-NN/tree.md` (traversable outline: header counts + indented hierarchy + ids + notes + `— thin` gaps) — **DONE** (`scripts/human-sim-mindchat.mjs`, 5 steps; edit-before-maximize added per mindmap-chat-hardening-2026-07-09 — stale-snapshot bugs are invisible to no-edit runs). Backend: `tests/mindmap-outline.test.mjs` proves `treeToOutline` + `tree.md` on present/response/draft + digest embed + mindmap draft reload | `npm run test:human:mindchat` + `node --test tests/mindmap-outline.test.mjs` (both gated in `npm test`) | Low — 2026-07-09: the mindmap board is presented by the REAL blocked `present_board` tool (kind="mindmap" + tree); the chat PARKS it (park response asserted at the orchestrator) and the answer is delivered via the real `reply_artifact_chat` under the snapshot artifact slug (bubble in-frame) while the board stays live; real mind-elixir canvas + real engine addChild; draft.json/tree.md asserted from disk. The orchestrator's tree IMPROVEMENT (read-mindmap → re-present an improved TREE, never an svg-artisan redraw — `.claude/commands/artifact-chat.md` mind-map branch) needs a live model. |
| **4. Archived thread reopen** | archived thread → replay + live composer → reopen control | Seeded `_completed/` thread's canonical round labels visible in replay; reopen control reachable by real nav — **DONE** | `npm run test:human:archived` (`scripts/human-sim-archived.mjs`, gated in `npm test`) | Low — seeds a real `_completed/` thread on disk and drives the real studio; keep the reopen a real navigation, not direct state. |
| **4b. Archived artifact chat (ask anytime)** | archived thread → click keep → ArtifactFullscreen composer → type a question → Send | The chat composer is PRESENT on the archived thread (not read-only); a newly typed question echoes back INTO the archived dialog and persists to that thread's own `artifacts/chat.jsonl` (never the live thread) — thread-addressed by `discussionId` — **DONE** (`scripts/human-sim-archived.mjs` step "ask a NEW question on the archived thread"). Backend proven on the real bridge + WS by `tests/api-status-matrix.test.mjs` (archived-thread record/route/404) | `npm run test:human:archived` + `node --test tests/api-status-matrix.test.mjs` (both gated in `npm test`) | Low — 2026-07-09: the question now RESOLVES the blocked `open_studio` as the artifact-chat command (discussionId in the seedNote, asserted at the orchestrator) and the answer is DELIVERED via the real `reply_artifact_chat` with that `discussionId` — Claude's bubble asserted in the archived dialog, persistence in the archived chat.jsonl, the live thread untouched. Only the reply's authoring needs a live model. |
| **5. Mind-map node controls** | live mind map → `+5` → note a node → explode → delete a node → submit | ALL engine-sourced (`mind.getData()`), no fixture-orchestrator: `+5` adds five REAL children under "Mark"; **EXPLODE fans "Motion" into ≥5 REAL children whose labels are STEERED by the note** (`…kinetic…`, a different note → a different set); **DELETE really removes a node** ("Mark" 5→4, the deleted id absent from the tree — proves the `removeNodes` fix over the old dead `removeNode?.()` no-op); `editedTree` + `treeOps` (add/explode/delete) + `round-NN/tree-ops.jsonl` reflect it — **DONE** | `npm run test:human` (step "EXPLODE really fans a node into ≥5 note-steered children", gated in `npm test`) | Low — explode/delete are now DETERMINISTIC studio actions (explode pre-seeds 5 topic+note-anchored prompt children; delete calls the real `removeNodes`), fully asserted from the live engine, never faked. A live `/run-brainstorm` where the model ENRICHES each prompt into a richer idea is a nice-to-have refinement, not a correctness gap — the feature works and is proven without a model in the loop. |
| **6. Decision tree** | live thread → wayfinder 🌳 toggle → decision-tree overlay | Overlay renders the REAL server-built `<svg>` whose **canonical `<text>` labels are in frame** — the mind-map round's `edited: Neon glyph` — **DONE**. ⚠️ FALSE-GREEN CAUGHT (2026-07-08): the prior `querySelector('svg')` check passed on App.tsx's `decision tree unavailable` error-fallback `<svg>` (a fetch failure looked identical to success); now asserts canonical content so a failed/empty fetch times out honestly | `npm run test:human` (step "the decision-tree overlay opens"), backend shape in `node scripts/smoke.mjs` (`/api/decision-tree/:id`) | Low — the SVG is built by `apps/mcp/src/decision-tree.ts` from the real reloaded thread, fetched over the real endpoint; nothing client-fabricated. |
| **7. Annotate a photo seed (expressive tools)** | New Discussion → pick a palette → attach an image → accept the offer → photo becomes the pad background → **Pen + Highlighter + Box + Text note + Arrow** in a palette color → **undo** a throwaway mark → **Maximize** (fullscreen input) → Send & iterate | The Aurora palette selects; the offer banner appears; the photo is the pad `<image>` background; a REAL CDP-drag **pen** inks in the chosen swatch (`stroke="#7c3aed"` read off the live pad), a **highlighter** renders translucent (`stroke-opacity="0.35"`), a **box** scopes a region (`<rect fill-opacity="0.12">`), a **note-overlay** `<text>` ("FOCUS HERE") renders, a drag **arrow** renders its `<polygon>`; **undo-last** removes exactly a throwaway 3rd polyline (back to 2, keepers intact); **Maximize** opens `[data-testid="scribble-fullscreen"]` with marks kept AND no artifact-chat composer (input-only), Minimize returns inline. **Send** writes a TRAVERSABLE `.seeds/seed-<stamp>/` FOLDER: a real `composite.png` (PNG magic header — a raster the model can SEE), `scribble.json` (all five mark types + note text verbatim + the mark's Aurora palette color NAME), plus `photo.png`/`scribble.svg`/`README.md`, and the raw photo ALSO persists as an `attachments/` file (Keep both) — all asserted off disk on the real bridge — **DONE** (`scripts/human-sim.mjs`, annotate + send steps). Also unit-proven: **DONE** (`scripts/ui-smoke.ts` — `composeSeedSvg`/`toScribbleAnnotations`: pen/highlighter/box/arrow/note, palette color names, well-formed rasterizable SVG, escaped note; `scripts/smoke.mjs` — the bridge writes the folder + a `/read-scribble` seedNote) | `npm run test:human` + `npm run smoke:ui` + `node scripts/smoke.mjs` (all gated in `npm test`) | Low — real CDP pointer drags on the real `PhotoScribble`; the note pointerdown is a real dispatched `PointerEvent` on the real canvas (headless CDP won't synthesize a stationary-tap pointerdown — same tradeoff as the mindmap step); the composite PNG is rasterized by the REAL browser and its bytes read off disk; nothing faked. |
| **8. Scribble intent anchors the brainstorm** | submit a scribble seed → orchestrator runs `/read-scribble` → VIEW `composite.png` + read `scribble.json` → write `🖼 Scribble intent` into `brainstorm.md` → round 1 + clarifications build on the marks | The seedNote instructs `/read-scribble` and names the folder; the folder is model-legible (composite.png VISION + scribble.json traversal + README) — **persistence + command wiring DONE** (`scripts/smoke.mjs` asserts the seedNote routes to `/read-scribble`; `scripts/human-sim.mjs` asserts the folder contents; the command is registered + referenced from run-brainstorm step 1 / the orchestrator front door / brainstorm-phases). The model ACTUALLY reading composite.png and anchoring round 1 on the marks (the `🖼 Scribble intent` block in `brainstorm.md`) — **OWED** (needs a live orchestrator; the browser harness has no model in the loop) | persistence: `node scripts/smoke.mjs`; wiring: the registry guard + a live `/run-brainstorm` drive with an attached scribble (OWED) | Low for persistence + wiring (asserted off disk + guard). The model-in-the-loop anchoring is OWED — same class as journeys #1/#5's live-model rows; documented, not faked. |
| **9. Token economy visible ("Where the tokens went")** | live thread → Session activity panel → per-sink bars | The always-visible "Where the tokens went" breakdown labels each nonzero sink (Generation / Tweak / Intake / Orchestration / Poster — the protocol's `TokenSink` enum) with a bar + compact count; zero-token sinks are OMITTED (no empty bars); the counts equal the thread's `progress.jsonl` per-sink sums (real turn deltas; attribution heuristic disclosed) — component labels **DONE** (`scripts/ui-smoke.ts` "per-sink accounting": labeled bars, zero sinks omitted, no panel without the prop); attribution semantics **DONE** on the real bridge (`scripts/smoke.mjs`: boundary label → next delta inherits, consume-once, uncategorized folds into orchestration, live/reload parity). A LIVE session's bars reflecting a real brainstorm's sinks end-to-end (hook-piped boundaries from a real `/run-brainstorm`) — **OWED**: measure during the token-economy live A/B, procedure + baseline in `discussion/_completed/token-economy-2026-07-07/baseline.md` | `npm run smoke:ui` + `node scripts/smoke.mjs` (both gated in `npm test`) | Low — the panel renders `/api/state.tokensBySink` computed by the real `SessionStore` from real hook deltas; nothing fabricated (the tokens are real, only the attribution is a heuristic and the UI/wiki disclose it; subagent under-count disclosed too). |
| **10. Claude asks (mid-round question)** | live board carrying `board.questions` → "Claude asks" box beside the options → pick chips / type → submit | The "Claude asks" box renders the question text VERBATIM beside the option grid; the answer rides `BoardResponse.questionAnswers` keyed by question id; `brainstorm.md`'s digest shows `Answer — "<question>": <answer>` with the QUESTION TEXT resolved (not the id) — **digest + schema DONE** (`tests/feedback.test.mjs` questionAnswers case, `tests/field-coverage.test.mjs` drift guard, protocol round-trip); the browser walk (box visibly rendered on a real presented board, answer typed by real input, digest observed after a real submit) — **OWED** (no sim drives it; first proof = the human-testing script in `discussion/_completed/handoff-fidelity-2026-07-09/plan.md`) | backend: `node --test tests/feedback.test.mjs`; walk: live `/run-brainstorm` (OWED) | Medium — the box only arises when the ORCHESTRATOR authors `questions` on `present_board`; a harness that injects them via the bridge would be a faked orchestrator. Prove on a live run. |
| **11. Unsure (never a silent kill)** | live board (grid/triage card) → `option-unsure` toggle (amber) → submit | The toggle flips visibly (aria-pressed, `unsure ✓`); the id rides `BoardResponse.uncertainties`; the digest emits the `UNSURE` line instructing a clarifying variant or a mid-round question — the option is NEVER treated as a kill — **digest + schema DONE** (`tests/feedback.test.mjs` unsure case); the browser walk + the NEXT round visibly addressing the unsure option — **OWED** (human test) | backend: `node --test tests/feedback.test.mjs`; walk: live `/run-brainstorm` (OWED) | Low for the toggle (deterministic studio state); the next-round clarifying behavior needs the live model. |
| **12. Remix recipe** | live board → mark TWO options remix → per-pair recipe input appears → type "take the layout of A, the palette of B" → submit | The recipe input appears only when a pair forms (keyed `a×b`); the text rides `BoardResponse.remixNotes`; the digest appends the recipe to the remix line so the model builds the hybrid from the STATED recipe, not a guess — **digest + schema DONE** (`tests/feedback.test.mjs` remix-recipe case); browser walk — **OWED** (human test) | backend: `node --test tests/feedback.test.mjs`; walk: live `/run-brainstorm` (OWED) | Low — deterministic studio state; the hybrid option itself needs the live model. |
| **13. Annotate ON an option** | live board → option fullscreen → **✏️ Annotate** toggle → the option SVG rasterizes into the scribble-pad background → arrow/box/highlight/pen/note in palette colors → close → submit | Marks land per-option in `BoardResponse.optionAnnotations` (structured, palette color NAMES); a rasterized `annotated-<optionId>.png` composite rides `attachments` and persists under the round (VISION-readable); the digest emits `Annotated ON "<label>"` (mark counts + note texts) + `VIEW <savedPath>`; drawing does NOT pan/zoom the viewer; toggling annotate off/on does not lose drafted marks — **digest + schema DONE** (`tests/feedback.test.mjs` optionAnnotations case); rasterize guards DONE (`scripts/ui-smoke.ts` "annotate guards": `withExplicitSize` injects width/height so Firefox/Safari don't draw the intrinsically-unsized option blank, `svgDims` reads either quote style + attr fallback so the aspect — and every mark coordinate — stays true); the browser walk (real drags on the pad OVER a real option SVG, the png bytes on disk, the digest VIEW path) — **OWED** (human-sim #7 proves the PhotoScribble pad at INTAKE only, not annotate-on-option) | backend: `node --test tests/feedback.test.mjs`; walk: live `/run-brainstorm` (OWED) | Medium — the pad is reused from #7 (proven), but rasterize-SVG-to-background + the attachments plumbing is new and only human/live-run proven. |
| **14. Rationale + lineage visible** | any board round ≥2 → option cards + fullscreen | Each option shows its `rationale` ("why I drew this", QUOTING the user's feedback) and lineage chips (↑ parent labels resolved from cached rounds) — the human SEES their feedback driving the options. Rendering wired (BoardSurvey + App `lineageLabels`), authoring taught (svg-artisan + brainstorm-phases) — **walk OWED** (needs a live orchestrator authoring `rationale`/`parents`; nothing deterministic can honestly produce them) | live `/run-brainstorm` (OWED) | High if faked — rationale/parents MUST come from the real orchestrator; never seed them in a harness. |
| **15. Crash-durability drill (the two-way exchange survives)** | mid-round: kill the bridge/session → restart → resume via `list_discussions` → continue | After restart: a response submitted pre-crash is returned by `peek_response` (disk fallback — never "pending" forever); a concierge answer / gallery pick posted with NO live waiter is stored and returned on re-call; queued UI commands reload from `.logs/pending-commands.jsonl`; the seedBrief reloads; a corrupt `board.json` skips that round honestly instead of bricking the thread; reopen moves the folder out of `_completed/` (bridge-performed, no manual `git mv`) — **backend DONE** (`tests/durability.test.mjs` crash-restart round-trips against a REAL Bridge + REAL disk; `scripts/smoke.mjs` reopen + structured concierge/gallery); the live drill (kill a REAL session mid-brainstorm, resume, verify nothing lost) — **OWED** (human test) | backend: `node --test tests/durability.test.mjs` + `node scripts/smoke.mjs`; live drill: human-testing script (OWED) | Low — the unit layer restarts a real Bridge on real disk state; the live drill adds only the full-process kill. |
| **16. Artifact kill→replace loop (in-progress-feedback)** | live thread → artifact streams in (`capture_artifact`) → open its fullscreen viewer → **Kill** with a note → shelf VISIBLY drops the chip, a shimmer placeholder (`pending-replacement`, "↻ replacing…") fills the slot → the queued `replace-artifact` command reaches `session_status` → a replacement (`capture_artifact` with `replaces`) AND an unrelated sibling capture land ASYNCHRONOUSLY → both chips render, placeholder gone → **Keep** on the replacement → "✓ Kept" persists across close/reopen | The killed artifact's slug chip DISAPPEARS from the shelf (not just a 200 — the href is gone from the DOM); the placeholder's `title` VISIBLY names the killed slug AND the exact kill note text; the replacement AND sibling chips each show their OWN real slug text in frame (order-independent — asserted regardless of which lands first); the killed sidecar gains `replacedBy` (disk-proven); the Keep badge ("✓ Kept", `aria-pressed`) survives a close/reopen (not a client-only toggle) — **DONE** (`scripts/human-sim-killreplace.mjs`, 10 steps, folds its own break-sweep: kill-form Cancel is a no-op, Escape in the kill-note textarea cancels the field not the viewer, and an empty-note kill on the already-kept REPLACEMENT chip proves the note optional + the chained shelf-slot model a hop deep) | `npm run test:human:killreplace` (gated in `npm test`) | Low — 2026-07-09: the thread is bound through the real `present_board` rearm route (no board waiting), every capture is the real `capture_artifact` MCP tool, every verdict the real `POST /api/artifact-verdict`, and the queued regeneration command is collected via the REAL `session_status` (never injected) before the "orchestrator" fulfills it with a real `capture_artifact {replaces}` call — mirroring how -livechat.mjs/-archived.mjs collect `artifact-chat` via `session_status` before `reply_artifact_chat`. Nothing calls the bridge's verdict/announce producers directly. |
| **17. Intake is chat history (no snap-back, nothing disappears)** | brief submit → concierge answer → THE GAP (no gallery/board yet) → intake history bubbles → 🌱 brief chip → gallery/board as normal | In the gap right after the concierge answer (the 2026-07-11 operator bug window: "my answer disappeared… anything I click takes me back to the new discussion"): the `intake-history` block shows the SUBMITTED BRIEF text, the concierge QUESTION, and the user's OWN ANSWER ("developers · shipping a CLI") as permanent bubbles; the New Discussion panel did NOT re-take the surface (no "Send & iterate" button in frame); the filmstrip's FIRST slot is the `intake-chip` (🌱 brief → jumps back to the history); the `intake-preparing` shimmer holds the gap honestly — **DONE** (`scripts/human-sim.mjs` step "answered intake STAYS in the chat"). Backend: `tests/intake-log.test.mjs` proves entry persistence (intake-log.json + reload), the WS `{type:'intake'}` broadcast, the timeout-is-not-logged honesty rule, the brief entry (rawBrief + survey ids for revise-prefill), that a brief over a thread WITH rounds is NOT logged locally (it travels with the command; the fresh brainstorm's landing loop records it), corrupt-entry tail survival, and `/api/discussions/:id` replay | `npm run test:human` + `node --test tests/intake-log.test.mjs` (both gated in `npm test`) | Low — the concierge/gallery/brief all arise from the REAL tool route (`ask_concierge`, `present_gallery`, the blocked `open_studio` resolving the panel submit); the intake entries are recorded by the real bridge/store, never injected. The "revise this brief" browser walk (✎ button → prefilled panel → fresh dispatch) is **OWED** (deterministic UI; first proof = next live `/run-brainstorm` or a sim extension). |
