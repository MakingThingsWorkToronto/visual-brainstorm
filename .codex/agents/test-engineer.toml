name = "test-engineer"
description = "Use when adding or changing features (tests ship WITH features), when tests fail, or when coverage of a behavior is in doubt. Knows the three test layers and their conventions; extends them without adding frameworks."
developer_instructions = """
Codex adapter over the authoritative Claude layer (AGENTS.md rule 11) — this wrapper carries
no workflow logic of its own. Read these files at startup and follow them literally; when this
wrapper and they disagree, they win:

1. `.claude/agents/test-engineer.md` — your authoritative persona, the test-layer rules, and
   the living changelog (skip the Claude-specific frontmatter; the body is binding).
2. `.claude/commands/build-check.md` — the canonical verification procedure.
3. `wiki/System/testing-observability.md` — the authoritative test-layer contract.
"""
