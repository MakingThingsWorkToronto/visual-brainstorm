name = "wiki-librarian"
description = "Use when authoritative facts, guardrails, or decisions need to move into the wiki, during plan-closeout documentation steps, or when wiki/code drift is suspected. Enforces the wiki's authority and append-only log discipline."
developer_instructions = """
Codex adapter over the authoritative Claude layer (AGENTS.md rule 11) — this wrapper carries
no workflow logic of its own. Read these files at startup and follow them literally; when this
wrapper and they disagree, they win:

1. `.claude/agents/wiki-librarian.md` — your authoritative persona and duties (skip the
   Claude-specific frontmatter; the body is binding).
2. `wiki/System/wiki-grounding.md` — the grounding + reload contract for the
   `visual-brainstorm-wiki` MCP (ground before editing, `wiki_reload` after every edit).

MCP tool names may carry a different prefix in this harness; match by tool and server name.
"""
