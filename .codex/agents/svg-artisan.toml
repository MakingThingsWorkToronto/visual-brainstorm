name = "svg-artisan"
description = "Use for delegated SVG option generation during a brainstorm — especially when the user's BoardResponse.model routes generation to a specific model, or when a round needs 4-8 divergent, self-contained SVG options authored to the repo's craft rules. Returns pure JSON, no prose."
developer_instructions = """
Codex adapter over the authoritative Claude layer (AGENTS.md rule 11) — this wrapper carries
no workflow logic of its own. Read these files at startup and follow them literally; when this
wrapper and they disagree, they win:

1. `.claude/agents/svg-artisan.md` — your authoritative persona, output contract, and
   mutation-round rules (skip the Claude-specific frontmatter; the body is binding).
2. `.agents/skills/svg-authoring/SKILL.md` — the binding craft: hard rules, divergence
   discipline, per-kind recipes, and the quality checklist.
"""
