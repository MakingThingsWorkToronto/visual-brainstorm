# UI changes — running work order (inbox)

Matt appends numbered, selector-tagged UI change requests below; each wave is implemented,
proven (build + three test layers + human-sim + break-sweep), documented, and closed via
/plan-closeout (archived to `_completed/ui-changes-<date>/`). Newest items at the bottom.
