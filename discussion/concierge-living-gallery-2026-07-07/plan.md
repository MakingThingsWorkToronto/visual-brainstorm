# Concierge → Living Gallery — mind-map methodology + intake flow
**Status:** open
**Goal:** The intake flow crowned in the brainstorm ships end-to-end: a brief submitted from the New Discussion panel is followed by an adaptive concierge (as many clarifying questions as it takes), answered by a Living Gallery of method cards seeded live from the brief + answers (recommended card ribboned with the quoted reason), and picking the new **Mind map** methodology opens a live co-edited mind-elixir canvas whose edits return to Claude as `editedTree`. Done = a human can drive that whole journey in the preview studio, every step verified by runnable tests.
**Source:** brainstorm thread `2026-07-06-2358-methodology-driven-intake-mind-map-board` (finalized round 4: option `concierge-living-gallery`; per-round decision blocks in its `brainstorm.md`) + `wiki/Research/visualization-engines.md` (mind-elixir/mindmapcn verdict). Killed directions (never build): seed-shape inference, structure dial, round-zero board grammar as the chooser, quiet auto-routing, combined questions+previews split view.

## Phases
| # | Phase | Goal + exit criteria (intent, no code) | Owner | Status |
|---|---|---|---|---|
| 1 | protocol-tree-payload | `tree` board payload (mind-elixir-compatible node tree) beside `options`, and `editedTree` response field, defined in packages/protocol FIRST (rule 5); MCP server and studio import the shapes, neither redeclares; exit: `npm run build` green + unit tests parsing/round-tripping a tree board and an editedTree response | test-engineer + inline | done |
| 2 | mindmap-live-board | Studio renders a `mindmap`-kind board carrying `tree` as a live co-edited mind-elixir canvas (bundled engine, light/dark aware); user edits return in `editedTree`; every presented tree is captured as SVG via the export path with provenance (rule 7) and sanitized on render (rule 8); exit: smoke round-trips a tree board → editedTree; ui-smoke markers for the canvas surface; an exported SVG artifact lands in the thread dir | inline (engine) + test-engineer | done |
| 3 | concierge-intake | After the New Discussion brief, an adaptive clarifying-questions dialogue — as many questions as it takes, not a fixed count (operator: "being comprehensive will reward the brainstorm"); tapped answers package into the seed digest the orchestrator receives; exit: smoke covers the Q&A exchange landing in the command digest; ui-smoke markers for the question/chips surface | inline + test-engineer | todo |
| 4 | living-gallery | The concierge's response surface is the Living Gallery: multiple method cards (Mind map, Funnel, Wreck, Cluster) each a live miniature genuinely seeded from the brief + answers; recommended card accent-ringed + ribboned with a reason chip quoting the answers; picking a card routes the session into that methodology; exit: ui-smoke markers (ribbon, reason chip, per-method minis) + gallery visible in `npm run preview` | inline + svg-artisan (card minis craft) | todo |
| 5 | methodology-routing | The agentic layer learns the roster: mind map is a methodology BESIDE funnel/wreck/cluster (never the centerpiece); prompt→recommendation heuristics live in the skills/commands (brainstorm-phases, run-brainstorm, brainstorm-orchestrator), not harness code (rule 11); exit: skill/command files updated with the routing rules + a preview session routed brief→mindmap end-to-end | brainstorm-orchestrator + wiki-librarian | todo |
| 6 | human-verification | Mandatory (operator mandate 2026-07-07): human-sim goal run driving brief → concierge answers → gallery pick → live canvas edit → editedTree received, PLUS a break-sweep iterating every button and input on the new surfaces; API side: every reachable status code of new/changed endpoints asserted against `tests/canonical/` expectations; exit: both runnable commands green with observable output | test-engineer | todo |
| 7 | docs | `wiki/user-guide.md` (+ its SVG diagrams) documents the new intake journey and mind-map methodology; wiki System/Product pages updated for the tree payload contract and methodology roster; one `wiki/log.md` line per page (rules 2, 12); exit: pages updated, log lines appended | wiki-librarian | todo |

## Progress log (append-only — every tick writes one line)
- 2026-07-07 01:55 — plan scaffolded by /plan-closeout step 7 from the finalized brainstorm (winner: concierge-living-gallery; poster + artifact captured in the thread's artifacts/).
- 2026-07-07 02:12 — protocol-tree-payload: MindNode/MindTree schemas + Board.tree + BoardResponse.editedTree in packages/protocol (options loosened to default [], invariant stays at present_board boundary); round-trip unit tests + canonical fixtures boards/mindmap-tree.json & responses/edited-tree.json (test-engineer); verify: npm run build → green; protocol/canonical/smoke/ui-smoke → green; npm test NOT fully green — api-status-matrix 2 fails (discussions/:id 200 body gained artifactChat key; artifact-chat 404 reworded) git-diff-attributed to the CONCURRENT ui-changes session's uncommitted apps/mcp diff, not this phase; commit 16338fb. Row stays in-progress pending a fully green npm test next tick.
- 2026-07-07 02:44 — protocol-tree-payload → done: concurrent session reconciled its canonical api expectations in the working tree; verify: npm run build → green; npm test (unit + smoke + ui-smoke) → exit 0, api-status-matrix 36/36; work commit 16338fb (prior tick).
- 2026-07-07 02:47 — loop stopped at operator request (session limit) BEFORE mindmap-live-board began — no phase 2 work exists; row 2 stays todo (clean resume point). Repo state: build + full npm test green at b1babea. Resume with `/loop /dispatch-concierge-living-gallery-next-phase`; phase 2 notes for the next session: protocol shapes ready (MindTree/editedTree, commit 16338fb), engine verdict + payload rationale in wiki/Research/visualization-engines.md, canonical fixtures boards/mindmap-tree.json + responses/edited-tree.json available for the smoke round-trip; concurrent ui-changes wave had a large UNCOMMITTED diff (apps/mcp + apps/studio) — see the concurrency learning in .agents/learnings.md before verifying.
- 2026-07-07 10:20 — mindmap-live-board → done (operator committed the two concurrent waves first, clearing the shared tree). Installed mind-elixir 5.13.0 in apps/studio (dynamically imported inside the mount effect → code-split 90.9kB chunk, keeps ui-smoke's esbuild clear of its .less). New MindmapCanvas renders the live co-edited canvas (light/dark via matchMedia); BoardSurvey routes kind==='mindmap'+tree to it (option grid/mechanics/axes suppressed, gate always open, edits lifted into response.editedTree). present_board accepts `tree`, enforces options-XOR-tree + no-axes-for-tree at the boundary. New apps/mcp/src/tree-svg.ts renders a deterministic XML-escaped SVG snapshot; recordBoard writes round-NN/tree.json + captures it as a provenance'd artifact (rule 7+8). App history shows a compact tree summary. Tests: tests/tree-svg.test.mjs (unit), smoke mindmap round-trip (editedTree + tree.json + snapshot artifact + reload + brainstorm.md), ui-smoke canvas surface. verify: npm run build → green; npm test → exit 0 (unit 126, SMOKE PASS, UI SMOKE PASS, HUMAN SIM PASS); commit be28285.
