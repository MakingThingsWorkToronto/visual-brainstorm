# Concierge → Living Gallery — mind-map methodology + intake flow
**Status:** open — 6/7 phases done (protocol-tree-payload, mindmap-live-board, concierge-intake, living-gallery, methodology-routing, human-verification). Next: **phase 7 docs** (last; then `/plan-closeout`).

> **OPERATOR MANDATE 2026-07-07 (real-session, not preview):** The intake (handoff → concierge → gallery → route → mind map) MUST work in REAL Claude-Code-driven brainstorms — "if it only works in preview the app is a brick." Where we stand: the MCP tools (`open_studio` brief, `ask_concierge`, `present_gallery`, `present_board` tree) ARE the real interface and are proven at the transport level by `scripts/smoke.mjs` (real `Bridge`, `engine:'claude'` — NOT the preview harness). The GAP: `run-brainstorm` / `brainstorm-orchestrator` / `brainstorm-phases` don't yet instruct Claude to CALL these tools, so a real session never triggers the new intake — that wiring is **phase 5**, and **phase 6** must prove a REAL end-to-end run (human-sim through brief→concierge→gallery→pick→mindmap edit→editedTree), never the preview fixture player. Preview is a bonus demo, never the proof. NOTE: a concurrent `agentic-model-efficiency` session is editing the same `.claude/` skills+commands — coordinate/serialize at phase 5.
**Goal:** The intake flow crowned in the brainstorm ships end-to-end: a brief submitted from the New Discussion panel is followed by an adaptive concierge (as many clarifying questions as it takes), answered by a Living Gallery of method cards seeded live from the brief + answers (recommended card ribboned with the quoted reason), and picking the new **Mind map** methodology opens a live co-edited mind-elixir canvas whose edits return to Claude as `editedTree`. Done = a human can drive that whole journey in the preview studio, every step verified by runnable tests.
**Source:** brainstorm thread `2026-07-06-2358-methodology-driven-intake-mind-map-board` (finalized round 4: option `concierge-living-gallery`; per-round decision blocks in its `brainstorm.md`) + `wiki/Research/visualization-engines.md` (mind-elixir/mindmapcn verdict). Killed directions (never build): seed-shape inference, structure dial, round-zero board grammar as the chooser, quiet auto-routing, combined questions+previews split view.

## Restart state (read FIRST in a new session)

- **How to resume:** run `/loop /dispatch-concierge-living-gallery-next-phase` (or invoke the `dispatch-concierge-living-gallery-next-phase` skill directly). It reads THIS file as its only state, picks the first `todo` row (phase 7, the LAST), and runs one phase per tick. When all rows are `done` it runs `/plan-closeout`.
- **Repo baseline:** clean & green at commit `5cfb4ac` (phase-6 harnesses). Prove before starting: `npm run build` + `npm test` → exit 0 (unit 133, SMOKE PASS, UI SMOKE PASS, HUMAN SIM PASS 12 steps); the real-session proof is `npm run test:human` (full intake) + `npm run test:human:sweep` (BREAK SWEEP PASS, 0 findings). If red, `git status` first — a CONCURRENT session's uncommitted diff is the usual cause; attribute before reacting, never commit another plan's work as a rider.
- **Already built (do NOT re-derive):**
  - Protocol (`packages/protocol/src/index.ts`): `MindNode`/`MindTree` + `Board.tree?`/`BoardResponse.editedTree?` (options loosened to `.default([])`, options-OR-tree invariant at the `present_board` boundary); `ConciergeExchange` + `MethodCard`/`LivingGallery` + `StudioState.seedBrief`/`.concierge`/`.gallery` + `ServerToStudio` `concierge` & `gallery` envelopes.
  - MCP (`apps/mcp/src`): `present_board` accepts `tree`; `tree-svg.ts` deterministic XML-escaped snapshot; `recordBoard` writes `tree.json` + captures a provenance'd artifact (rule 7+8). `open_studio` accepts `brief` (handoff → seedBrief); `ask_concierge` + `bridge.askConcierge` + `POST /api/concierge`; `present_gallery` + `bridge.presentGallery` + `POST /api/gallery-pick`; `store.recordConcierge`/`recordGalleryPick` append to brainstorm.md. `preview.ts` shows the gallery fixture once before the phase tour.
  - Studio (`apps/studio/src`): `MindmapCanvas.tsx` (live mind-elixir 5.13, **dynamically imported inside the mount effect** — keeps ui-smoke's esbuild clear of its `.less`); `BoardSurvey.tsx` routes `kind==='mindmap' && board.tree`; `ConciergeIntake.tsx`; `LivingGallery.tsx` (method cards, recommended ribbon + reason chip, per-method minis, pick → `pickMethod`); `NewDiscussionPanel.tsx` `initialPrompt` pre-fills the brief; `App.tsx` renders concierge + gallery surfaces; `useBridge.ts` handles `concierge`/`gallery` envelopes + `answerConcierge`/`pickMethod`.
  - Tests: `tests/tree-svg.test.mjs`; smoke mindmap + concierge + handoff + gallery round-trips; ui-smoke canvas + concierge + prefill + gallery surfaces; api-status-matrix `POST /api/concierge` + `POST /api/gallery-pick` (200/404/400); canonical `boards/mindmap-tree.json`, `responses/edited-tree.json`, `gallery/gallery.json` (4 svg-artisan method minis).
  - **Routing (phase 5, real-session wiring):** `.claude/commands/run-brainstorm.md` step 0 = concierge→gallery intake (handoff→ask_concierge→present_gallery→route pick); `.claude/agents/brainstorm-orchestrator.md` keeps the intake front door + delegates gallery minis; `.claude/skills/brainstorm-phases/SKILL.md` carries the methodology roster + routing table (mindmap = peer, never default). `apps/mcp/src/preview.ts` routes a mindmap pick → live tree board. Wiki as-built: `wiki/Product/intake-methodologies.md` (BUILT), `wiki/Requirements/interaction-protocol.md` (tools roster).
  - **Real-session proof (phase 6):** `scripts/human-sim.mjs` drives the full intake in a real browser (12 steps, engine:claude); `scripts/ui-break-sweep.mjs` sweeps the concierge/gallery/mindmap surfaces (0 findings). `MindmapCanvas` exposes `el.mind` (interop handle) so the live engine edit is driveable. `npm run test:human` + `npm run test:human:sweep`.
- **Phase 7 entry notes (docs) — the LAST phase before closeout:** update `wiki/user-guide.md` (+ its SVG diagrams) to document the NEW intake journey for a human reader: how a brainstorm now opens (Claude Code handoff pre-fills the brief → concierge asks a few clarifying questions → the Living Gallery offers method cards with a recommendation → picking Mind map opens a live co-edited canvas). The wiki System/Product contract pages were ALREADY reconciled in phase 5 (`intake-methodologies.md`, `interaction-protocol.md`) — for phase 7 confirm they're still accurate and focus on the user-guide (rules 2, 12: one `wiki/log.md` line per page edited). Owner: wiki-librarian. Exit: user-guide documents the journey (with diagrams), log lines appended. Then ALL rows done → the tick runs `/plan-closeout` (verify → harvest learnings → improve the commands/skills the learnings implicate → wiki → archive the plan folder to `_completed/`; closeout step 7 offers the target-repo build plan). Reconcile `intake-methodologies.md` status from "plan OPEN" to closed as the final closeout step.
- **Universal discipline:** one phase per tick; delegate to owning agent/skill (rule 11); tests ship WITH the work (rule 10); commit `--only` the exact phase paths (never `-A`); then commit the plan tick separately.

## Phases
| # | Phase | Goal + exit criteria (intent, no code) | Owner | Status |
|---|---|---|---|---|
| 1 | protocol-tree-payload | `tree` board payload (mind-elixir-compatible node tree) beside `options`, and `editedTree` response field, defined in packages/protocol FIRST (rule 5); MCP server and studio import the shapes, neither redeclares; exit: `npm run build` green + unit tests parsing/round-tripping a tree board and an editedTree response | test-engineer + inline | done |
| 2 | mindmap-live-board | Studio renders a `mindmap`-kind board carrying `tree` as a live co-edited mind-elixir canvas (bundled engine, light/dark aware); user edits return in `editedTree`; every presented tree is captured as SVG via the export path with provenance (rule 7) and sanitized on render (rule 8); exit: smoke round-trips a tree board → editedTree; ui-smoke markers for the canvas surface; an exported SVG artifact lands in the thread dir | inline (engine) + test-engineer | done |
| 3 | concierge-intake | After the New Discussion brief, an adaptive clarifying-questions dialogue — as many questions as it takes, not a fixed count (operator: "being comprehensive will reward the brainstorm"); tapped answers package into the seed digest the orchestrator receives; exit: smoke covers the Q&A exchange landing in the command digest; ui-smoke markers for the question/chips surface | inline + test-engineer | done |
| 4 | living-gallery | The concierge's response surface is the Living Gallery: multiple method cards (Mind map, Funnel, Wreck, Cluster) each a live miniature genuinely seeded from the brief + answers; recommended card accent-ringed + ribboned with a reason chip quoting the answers; picking a card routes the session into that methodology; exit: ui-smoke markers (ribbon, reason chip, per-method minis) + gallery visible in `npm run preview` | inline + svg-artisan (card minis craft) | done |
| 5 | methodology-routing | The agentic layer learns the roster: mind map is a methodology BESIDE funnel/wreck/cluster (never the centerpiece); prompt→recommendation heuristics live in the skills/commands (brainstorm-phases, run-brainstorm, brainstorm-orchestrator), not harness code (rule 11); exit: skill/command files updated with the routing rules + a preview session routed brief→mindmap end-to-end | brainstorm-orchestrator + wiki-librarian | done |
| 6 | human-verification | Mandatory (operator mandate 2026-07-07): human-sim goal run driving brief → concierge answers → gallery pick → live canvas edit → editedTree received against a REAL bridge (`engine:'claude'`, NOT the preview fixture player — "if it only works in preview the app is a brick"), PLUS a break-sweep iterating every button and input on the new surfaces; API side: every reachable status code of new/changed endpoints (`/api/concierge`, `/api/gallery-pick`, tree boards) asserted against `tests/canonical/` expectations; exit: both runnable commands green with observable output driving the real intake end-to-end | test-engineer | done |
| 7 | docs | `wiki/user-guide.md` (+ its SVG diagrams) documents the new intake journey and mind-map methodology; wiki System/Product pages updated for the tree payload contract and methodology roster; one `wiki/log.md` line per page (rules 2, 12); exit: pages updated, log lines appended | wiki-librarian | todo |

## Progress log (append-only — every tick writes one line)
- 2026-07-07 01:55 — plan scaffolded by /plan-closeout step 7 from the finalized brainstorm (winner: concierge-living-gallery; poster + artifact captured in the thread's artifacts/).
- 2026-07-07 02:12 — protocol-tree-payload: MindNode/MindTree schemas + Board.tree + BoardResponse.editedTree in packages/protocol (options loosened to default [], invariant stays at present_board boundary); round-trip unit tests + canonical fixtures boards/mindmap-tree.json & responses/edited-tree.json (test-engineer); verify: npm run build → green; protocol/canonical/smoke/ui-smoke → green; npm test NOT fully green — api-status-matrix 2 fails (discussions/:id 200 body gained artifactChat key; artifact-chat 404 reworded) git-diff-attributed to the CONCURRENT ui-changes session's uncommitted apps/mcp diff, not this phase; commit 16338fb. Row stays in-progress pending a fully green npm test next tick.
- 2026-07-07 02:44 — protocol-tree-payload → done: concurrent session reconciled its canonical api expectations in the working tree; verify: npm run build → green; npm test (unit + smoke + ui-smoke) → exit 0, api-status-matrix 36/36; work commit 16338fb (prior tick).
- 2026-07-07 02:47 — loop stopped at operator request (session limit) BEFORE mindmap-live-board began — no phase 2 work exists; row 2 stays todo (clean resume point). Repo state: build + full npm test green at b1babea. Resume with `/loop /dispatch-concierge-living-gallery-next-phase`; phase 2 notes for the next session: protocol shapes ready (MindTree/editedTree, commit 16338fb), engine verdict + payload rationale in wiki/Research/visualization-engines.md, canonical fixtures boards/mindmap-tree.json + responses/edited-tree.json available for the smoke round-trip; concurrent ui-changes wave had a large UNCOMMITTED diff (apps/mcp + apps/studio) — see the concurrency learning in .agents/learnings.md before verifying.
- 2026-07-07 20:07 — human-verification → done. THE real-session proof (operator mandate). Extended `scripts/human-sim.mjs` (real chrome/CDP + real bridge, engine:claude — NOT preview): the goal run now drives brief → concierge Q&A (tap chip + type + Send answer, askConcierge resolves) → Living Gallery (recommended ribbon renders live; click Mind map card, presentGallery resolves 'mindmap') → live mindmap board (the mind-elixir engine mounts REAL DOM — first place the genuine engine runs, never under renderToString) → a genuine engine edit (`mind.selectNode` + `await mind.addChild()` via the exposed `el.mind` interop handle) → Send → `editedTree` round-trips with the added node → existing board+artifact steps still pass. 12 steps, zero exceptions/client-errors. Extended `scripts/ui-break-sweep.mjs` with concierge/gallery/mindmap surfaces (434 controls, 525 gestures, 0 findings across 10 surfaces). Added a minimal instance-exposure handle to `MindmapCanvas` (mind-elixir interop convention) so a real edit is driveable — editedTree comes from the real engine→onEdit path, never fabricated. API side already covered (api-status-matrix /api/concierge + /api/gallery-pick 200/404/400). verify: npm run build → green; npm run test:human → HUMAN SIM PASS (12 steps); npm run test:human:sweep → BREAK SWEEP PASS (0 findings); npm test → exit 0; commit 5cfb4ac.
- 2026-07-07 19:22 — methodology-routing → done (the REAL-SESSION wiring the operator mandated). BLOCKER first: the concurrent `agentic-model-efficiency` session had stalled ~50min with its COMPLETE-but-uncommitted model-tiering work frozen on the exact `.claude` files phase 5 needs; operator said "should be committed now" → I committed that session's finished work on its behalf (commit 9877356, attributed) to clear the tree. Then phase 5: `run-brainstorm.md` step 0 rewritten as the concierge→gallery intake (handoff brief→open_studio, adaptive ask_concierge, present_gallery w/ svg-artisan minis + one recommendation, route the pick); `brainstorm-orchestrator.md` keeps the intake front door + delegates gallery minis; `brainstorm-phases/SKILL.md` gains the methodology roster + routing table (mindmap is a PEER methodology, never default). `preview.ts` routes a mindmap pick → live tree board. Wiki reconciled to as-built (wiki-librarian: intake-methodologies.md BUILT, interaction-protocol.md +2 tools, log ×2) — corrected its premature "plan closed" overstatement (phases 6–7 remain). REAL-SESSION ROUTE PROVEN end-to-end vs the real bridge (engine:claude) via a scratchpad verify: gallery pick "mindmap" → tree board (kind mindmap, tree carried) → editedTree returned. verify: npm run build → green; npm test → exit 0 (unit 133, SMOKE PASS, UI SMOKE PASS, HUMAN SIM PASS); commits 7d1d76f (code/skills) + 971a079 (wiki).
- 2026-07-07 12:03 — living-gallery → done. New `MethodCard`/`LivingGallery` protocol shapes + `gallery` WS envelope + `StudioState.gallery`; `present_gallery` MCP tool + `bridge.presentGallery` (blocking, like present_board) + `POST /api/gallery-pick` (200/404/400, method must be a card); `store.recordGalleryPick` → brainstorm.md. New `LivingGallery.tsx` studio surface (per-method minis via SvgPane, recommended card accent-ring + `recommended-ribbon` + `reason-chip`, pick → `pickMethod`). svg-artisan crafted 4 method minis (mindmap/funnel/wreck/cluster) → canonical `tests/canonical/gallery/gallery.json` (single source; preview harness reads it to show the gallery once before the phase tour). Tests (test-engineer): smoke gallery round-trip, ui-smoke gallery surface, api-status-matrix new endpoint, canonical fixture registered, state-200.json +gallery key. verify: npm run build → green; npm test → exit 0 (unit 133, SMOKE PASS, UI SMOKE PASS, HUMAN SIM PASS); commit dabd061. OPERATOR MANDATE captured (header): the intake must work in REAL sessions — MCP tools proven vs real bridge by smoke; phase 5 wires the skills to call them, phase 6 proves real e2e; preview ≠ proof. Concurrent `agentic-model-efficiency` session editing `.claude/**`+`wiki/**` (phase-5 files) — serialize.
- 2026-07-07 11:31 — concierge-intake → done. TWO deliverables (operator added the handoff requirement mid-tick): (1) Claude Code → studio HANDOFF — `open_studio` gained a `brief` param → `bridge.openStudio(brief, open?)` sets `StudioState.seedBrief`; `NewDiscussionPanel` gained `initialPrompt` (seeded once via a ref-guarded effect so async WS arrival never clobbers user edits) so the studio hosts the purpose the human already described (no retyping). (2) Adaptive CONCIERGE Q&A — new `ConciergeExchange` protocol shape + `concierge` WS envelope; `ask_concierge` MCP tool + `bridge.askConcierge` (blocking like present_board, as many turns as it takes) + `POST /api/concierge` (200/404/400); new `ConciergeIntake` studio surface (question + tappable suggestion chips + free text); each answered exchange appends to brainstorm.md via `store.recordConcierge` (the digest the orchestrator builds on). Operator was consulted first re: the blocked shared tree — they committed the two concurrent waves, clearing it. Tests (test-engineer): smoke handoff + Q&A round-trip; ui-smoke both surfaces + prefill; api-status-matrix new endpoint; canonical state-200.json gained seedBrief/concierge + 3 concierge-*.json bodies. verify: npm run build → green; npm test → exit 0 (unit 129, SMOKE PASS, UI SMOKE PASS, HUMAN SIM PASS); commit edd66bf.
- 2026-07-07 10:20 — mindmap-live-board → done (operator committed the two concurrent waves first, clearing the shared tree). Installed mind-elixir 5.13.0 in apps/studio (dynamically imported inside the mount effect → code-split 90.9kB chunk, keeps ui-smoke's esbuild clear of its .less). New MindmapCanvas renders the live co-edited canvas (light/dark via matchMedia); BoardSurvey routes kind==='mindmap'+tree to it (option grid/mechanics/axes suppressed, gate always open, edits lifted into response.editedTree). present_board accepts `tree`, enforces options-XOR-tree + no-axes-for-tree at the boundary. New apps/mcp/src/tree-svg.ts renders a deterministic XML-escaped SVG snapshot; recordBoard writes round-NN/tree.json + captures it as a provenance'd artifact (rule 7+8). App history shows a compact tree summary. Tests: tests/tree-svg.test.mjs (unit), smoke mindmap round-trip (editedTree + tree.json + snapshot artifact + reload + brainstorm.md), ui-smoke canvas surface. verify: npm run build → green; npm test → exit 0 (unit 126, SMOKE PASS, UI SMOKE PASS, HUMAN SIM PASS); commit be28285.
